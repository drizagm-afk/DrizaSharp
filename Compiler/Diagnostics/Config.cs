/*
namespace DrzSharp.Compiler.Diagnostics;

public sealed record Config(
    OutputMode Output,
    LexerConfig Lexer,
    ParserConfig Parser,
    LowererConfig Lowerer
);
public enum OutputMode
{ CONSOLE, DZDIAG }

public sealed record LexerConfig(
    bool ShowLogs,
    bool ShowTokenList
);

public sealed record ParserConfig(
    bool ShowLogs,
    TASTMode ShowTAST
);
public enum TASTMode
{ Hidden, TextOriented, TokenOriented }

public sealed record LowererConfig(
    bool ShowLogs,
    bool ShowTASI
);
*/