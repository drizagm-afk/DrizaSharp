#RUN {
    int x = 10
    if (x > 5) println("big")
    elif (x == 5) println("perfect")
    else println("small")

    while(false) { }
    repeat(10) println("Hello")
    for(int i = 0; i < 10; i++) println(i)
    foreach(i in 0..10) println(i)

    match(value) {
        0 => println("zero")
        1 => println("one")
        > 5 => println("big")
        _ => println("other")
    }
    var msg = match(value) {
        0, 1 => "allowed"
        @greater 5 => "big"
        _ => "other"
    }
}

struct Vec2(float[2] values) {
    int X { get, set => values[0] }
    int Y { get => values[0], set => values[0] = value }

    int Add(int a, int b) => a + b
}